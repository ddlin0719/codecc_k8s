apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: bkci-codecc-task
  name: bkci-codecc-task
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: bkci-codecc-task
  template:
    metadata:
      labels:
        app: bkci-codecc-task
    spec:
      containers:
        - image: {{ .Values.image.hub }}/bkci-codecc-task:{{ .Values.image.tag }}
          name: bkci-notify
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          workingDir: /data/docker/bkci/ci/backend/
          command: ['/bin/sh','-c','/data/docker/bkci/ci/backend/module_run_codecc.sh']
          env:
            - name: CONSUL_SERVER
              value: {{ .Values.consul.server }}
            - name: NODE_IP
              valueFrom:
                fieldRef:
                    fieldPath: status.hostIP